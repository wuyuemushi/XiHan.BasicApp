import{_ as a,o as e,c as n,a3 as t}from"./chunks/framework.B5WAEDB7.js";const k=JSON.parse('{"title":"前端架构","description":"","frontmatter":{},"headers":[],"relativePath":"frontend/introduction.md","filePath":"frontend/introduction.md","lastUpdated":1786258579000}'),d={name:"frontend/introduction.md"};function i(p,s,o,c,l,r){return e(),n("div",null,[...s[0]||(s[0]=[t(`<h1 id="前端架构" tabindex="-1">前端架构 <a class="header-anchor" href="#前端架构" aria-label="Permalink to &quot;前端架构&quot;">​</a></h1><p>前端不是「一个 Vue 工程」，而是<strong>分层的单仓库多包结构</strong>。本页讲分层与依赖方向、启动引导链路、请求链路的完整时序，以及第三方依赖的归属规则。具体怎么写页面见 <a href="./development">开发流程</a>。</p><h2 id="技术栈" tabindex="-1">技术栈 <a class="header-anchor" href="#技术栈" aria-label="Permalink to &quot;技术栈&quot;">​</a></h2><table tabindex="0"><thead><tr><th>领域</th><th>选型</th></tr></thead><tbody><tr><td>框架</td><td>Vue 3.5（<code>&lt;script setup&gt;</code>）</td></tr><tr><td>语言 / 构建</td><td>TypeScript · Vite</td></tr><tr><td>UI 库</td><td>Naive UI</td></tr><tr><td>状态</td><td>Pinia（Setup Store + <code>$reset</code> 插件）</td></tr><tr><td>样式</td><td>Tailwind CSS 4（CSS-first <code>@theme</code>，<strong>preflight 关闭</strong>）</td></tr><tr><td>路由</td><td>vue-router（默认 hash 模式）</td></tr><tr><td>国际化</td><td>vue-i18n（<code>legacy: false</code>）</td></tr><tr><td>实时</td><td><code>@microsoft/signalr</code></td></tr><tr><td>图标</td><td>Iconify（<strong>离线模式</strong>）</td></tr><tr><td>拖拽</td><td><code>@dnd-kit/vue</code></td></tr></tbody></table><blockquote><p>版本号以仓库 <code>package.json</code> 为准。</p></blockquote><h2 id="五层结构" tabindex="-1">五层结构 <a class="header-anchor" href="#五层结构" aria-label="Permalink to &quot;五层结构&quot;">​</a></h2><p><strong>依赖方向单向向下，<code>packages/</code> 绝不反向依赖 <code>src/</code>。</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│  ⑤ 业务视图层  src/views/**                                       │</span></span>
<span class="line"><span>│     按域分目录：approval / develop / file / identity / log /      │</span></span>
<span class="line"><span>│     message / oauth / openapi / setting / tenant / workbench /    │</span></span>
<span class="line"><span>│     workflow                                                      │</span></span>
<span class="line"><span>│     每个列表页 = 一份 PageSchema + 一个 &lt;SchemaPage&gt;              │</span></span>
<span class="line"><span>├──────────────────────────────────────────────────────────────────┤</span></span>
<span class="line"><span>│  ④ 应用装配层  src/main.ts · App.vue · app/context.ts · router/  │</span></span>
<span class="line"><span>│     引导顺序、Provider 装配、把 api 实例 / 视图 glob / 静态路由    │</span></span>
<span class="line"><span>│     注册进 app-context，安装路由守卫                              │</span></span>
<span class="line"><span>├──────────────────────────────────────────────────────────────────┤</span></span>
<span class="line"><span>│  ③ 接口契约层  src/api/**                                         │</span></span>
<span class="line"><span>│     base（动态 API 客户端）· factory（资源工厂）· helpers（分页）  │</span></span>
<span class="line"><span>│     modules/**（按域的 API 与 DTO 类型）                          │</span></span>
<span class="line"><span>├──────────────────────────────────────────────────────────────────┤</span></span>
<span class="line"><span>│  ② 可复用内核  packages/**                                        │</span></span>
<span class="line"><span>│     components(schema/chat/common) · composables · hooks ·        │</span></span>
<span class="line"><span>│     stores · layouts · router · locales · design · iconify ·      │</span></span>
<span class="line"><span>│     diagram · plugins · views · types · utils · constants         │</span></span>
<span class="line"><span>├──────────────────────────────────────────────────────────────────┤</span></span>
<span class="line"><span>│  ① 传输底座  packages/request                                     │</span></span>
<span class="line"><span>│     RequestClient：请求头注入 / 响应解包 / 401 刷新重放 /          │</span></span>
<span class="line"><span>│     423 锁屏 / 可选签名加密                                        │</span></span>
<span class="line"><span>└──────────────────────────────────────────────────────────────────┘</span></span>
<span class="line"><span>                              ↕ HTTP + SignalR</span></span>
<span class="line"><span>                        后端动态 API（无 Controller）</span></span></code></pre></div><p>两个路径别名贯穿全项目：</p><table tabindex="0"><thead><tr><th>别名</th><th>指向</th><th>层</th></tr></thead><tbody><tr><td><code>@</code></td><td><code>src/</code></td><td>③④⑤ 应用侧</td></tr><tr><td><code>~</code></td><td><code>packages/</code></td><td>①② 内核侧</td></tr></tbody></table><h3 id="为什么-src-薄、packages-厚" tabindex="-1">为什么 <code>src/</code> 薄、<code>packages/</code> 厚 <a class="header-anchor" href="#为什么-src-薄、packages-厚" aria-label="Permalink to &quot;为什么 \`src/\` 薄、\`packages/\` 厚&quot;">​</a></h3><p><code>packages/</code> 里的东西<strong>与具体业务无关</strong>：布局、Schema 表格引擎、请求客户端、主题、i18n、图标、状态管理骨架。<code>src/</code> 只做两件事：装配（把内核接起来）和业务视图。</p><p>这样分的好处是内核可以被独立演进甚至复用，而业务视图始终只依赖稳定的契约。代价是<strong>新人容易找错文件</strong>——记住：找「怎么渲染表格 / 怎么发请求 / 怎么切主题」去 <code>packages/</code>，找「某个页面长什么样」去 <code>src/views/</code>。</p><h3 id="第三方依赖的归属规则" tabindex="-1">第三方依赖的归属规则 <a class="header-anchor" href="#第三方依赖的归属规则" aria-label="Permalink to &quot;第三方依赖的归属规则&quot;">​</a></h3><ul><li><strong>底层功能依赖归属对应的 <code>packages/</code> 子包</strong>（自包含）。</li><li>根 <code>package.json</code> 只留应用层跨切面依赖（<code>vue</code> / <code>vue-router</code> / <code>pinia</code> / <code>@vueuse</code> 因 Vite AutoImport 全局导入不可下沉，加上 <code>naive-ui</code> / <code>vue-i18n</code>）。</li><li><strong><code>src/</code> 里禁止直接 import 已下沉的第三方</strong>，一律经 <code>~/</code> 转出。</li><li>改完依赖归属须跑 <code>pnpm install</code> + <code>pnpm type-check</code>；死依赖当场删。</li></ul><h2 id="启动引导链路" tabindex="-1">启动引导链路 <a class="header-anchor" href="#启动引导链路" aria-label="Permalink to &quot;启动引导链路&quot;">​</a></h2><p><code>src/main.ts</code> 的顺序决定了「图标、语言、请求头、登出钩子、路由守卫」在挂载前如何就位：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setupIconifyOffline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ① 预加载离线图标集（必须最先，否则首屏图标空白）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">invalidateCacheIfBuildTimeChanged</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ② 构建时间变则清本地缓存</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// createApp → pinia → setupI18n</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // ③ 应用实例 / 状态 / 国际化</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bindRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(router)                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ④ 请求层拿到 router（401 跳登录用）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bindLogoutHook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 重置 access / user store */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerApplicationContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(router)    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ⑤ 注册视图 glob、api 实例、静态路由到 app-context</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setupRouterGuard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(router)              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ⑥ 安装 beforeEach 权限/路由守卫</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">use</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(router).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;#app&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>第 ④⑤ 步是<strong>依赖倒置的关键</strong>：<code>packages/request</code> 不能反向依赖 <code>src/router</code> 与业务 store，所以由 <code>src/main.ts</code> 在启动时把 router 和登出回调「注入」给它。同理 <code>app/context.ts</code> 把 api 实例、视图 glob、静态路由注册进 <code>~/stores/app-context</code>，底层包据此工作而不必知道 <code>src/</code> 的存在。</p><h2 id="请求链路时序" tabindex="-1">请求链路时序 <a class="header-anchor" href="#请求链路时序" aria-label="Permalink to &quot;请求链路时序&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>视图 (SchemaPage)</span></span>
<span class="line"><span>  └─ resource.page(params)                       ← 页面自己写的适配器</span></span>
<span class="line"><span>       └─ src/api/modules/**  positionApi.page()</span></span>
<span class="line"><span>            └─ createDynamicApiClient(&#39;PositionQuery&#39;).post(&#39;PositionPage&#39;, body)</span></span>
<span class="line"><span>                 │      URL = /{apiPrefix}/{控制器名}/{动作名}</span></span>
<span class="line"><span>                 └─ packages/request  RequestClient.post()</span></span>
<span class="line"><span>                      ├─ 请求拦截</span></span>
<span class="line"><span>                      │    Authorization: Bearer &lt;token&gt;</span></span>
<span class="line"><span>                      │    X-Timezone   （已选时区 / 浏览器 Intl）</span></span>
<span class="line"><span>                      │    X-Language   （当前 locale）</span></span>
<span class="line"><span>                      │    X-Request-Id （前端自生成，供本地请求日志面板）</span></span>
<span class="line"><span>                      │    FormData 时删除 Content-Type 让浏览器带 boundary</span></span>
<span class="line"><span>                      │    （可选）API 签名与请求体加密</span></span>
<span class="line"><span>                      ▼</span></span>
<span class="line"><span>                    后端动态 API</span></span>
<span class="line"><span>                      ▲</span></span>
<span class="line"><span>                      ├─ 响应拦截</span></span>
<span class="line"><span>                      │    （可选）解密 → 记本地请求日志（含 traceId）</span></span>
<span class="line"><span>                      │    423 → 触发锁屏遮罩钩子（必须先于 401 分支，且不刷新不登出）</span></span>
<span class="line"><span>                      │    401 → 刷新令牌并重放原请求</span></span>
<span class="line"><span>                      │           · 并发请求排队等同一次刷新</span></span>
<span class="line"><span>                      │           · 刷新请求自带 _isRefresh 标记，不会再次触发刷新</span></span>
<span class="line"><span>                      │           · 刷新失败 → 清 token + 调 logout 钩子 + 跳登录页</span></span>
<span class="line"><span>                      └─ 解包 ApiResponse 信封</span></span>
<span class="line"><span>                           isSuccess === true → 返回 data（可能是 null）</span></span>
<span class="line"><span>                           否则 reject(new Error(message))</span></span>
<span class="line"><span>                 ← 业务代码拿到的是**已解包的强类型 data**</span></span></code></pre></div><p>两个要点：</p><ol><li><strong>业务代码永远看不到 <code>ApiResponse</code> 信封</strong>。解包统一在 <code>RequestClient.request</code> 完成，失败时把可展示的中文提示抛成 <code>Error</code>，业务侧 <code>catch</code> 直接用 <code>error.message</code>。</li><li><strong>判定成功用 <code>isSuccess</code>，不是「有没有 <code>data</code> 字段」</strong>——服务端 <code>WhenWritingNull</code> 会把 <code>data: null</code> 整个省略。</li></ol><h2 id="环境变量" tabindex="-1">环境变量 <a class="header-anchor" href="#环境变量" aria-label="Permalink to &quot;环境变量&quot;">​</a></h2><table tabindex="0"><thead><tr><th>变量</th><th>开发默认</th><th>说明</th></tr></thead><tbody><tr><td><code>VITE_PORT</code></td><td><code>9800</code></td><td>dev server 端口</td></tr><tr><td><code>VITE_API_BASE_URL</code></td><td>空</td><td>协议+主机。<strong>开发态留空</strong>走同源 Vite 代理，天然无 CORS</td></tr><tr><td><code>VITE_API_PREFIX</code></td><td><code>/api</code></td><td>接口前缀</td></tr><tr><td><code>VITE_DEV_PROXY_TARGET</code></td><td><code>http://localhost:9708</code></td><td>开发态代理目标（后端地址）</td></tr><tr><td><code>VITE_ROUTER_HISTORY</code></td><td><code>hash</code></td><td>路由模式</td></tr><tr><td><code>VITE_HOME_PATH</code></td><td><code>/workbench/dashboard</code></td><td>首页兜底路径（后端菜单未派生出首页时用）</td></tr><tr><td><code>VITE_AUTH_ROUTE_MODE</code></td><td>动态</td><td>设为 <code>static</code> 则用前端静态路由 + 权限过滤</td></tr><tr><td><code>VITE_APP_TITLE</code> / <code>SUBTITLE</code> / <code>LOGO</code></td><td>—</td><td>品牌兜底（登录后由 <code>/Auth/UserInfo</code> 的 <code>appTitle</code> / <code>appLogo</code> 覆盖）</td></tr><tr><td><code>VITE_API_SECURITY_*</code></td><td><code>ENABLED=false</code></td><td>前端侧接口签名/加密开关与密钥</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">改了后端端口</p><p>要同步改 <code>frontend/.env.development</code> 的 <code>VITE_DEV_PROXY_TARGET</code>，否则 dev server 转发到旧端口。</p></div><h2 id="后端驱动的部分" tabindex="-1">后端驱动的部分 <a class="header-anchor" href="#后端驱动的部分" aria-label="Permalink to &quot;后端驱动的部分&quot;">​</a></h2><p>前端有相当一部分「事实源在后端」，这是理解整体架构的关键：</p><table tabindex="0"><thead><tr><th>东西</th><th>事实源</th><th>前端做什么</th></tr></thead><tbody><tr><td>菜单 / 路由 / 组件路径</td><td>后端 <code>PageRegistry</code> → <code>SysMenu</code></td><td>登录后拉 <code>/api/Auth/Permissions</code> 拿 <code>menus</code>，<code>mapMenuToRoutes</code> 转成路由并 <code>addRoute</code></td></tr><tr><td>权限码</td><td>后端 <code>SaasPermissionDefinitions</code></td><td>拿到码集合，按页面/字段/操作三级过滤</td></tr><tr><td>枚举标签</td><td>后端枚举元数据（<code>Enums.{culture}.json</code>）</td><td>按 <code>X-Language</code> 拉取，切语言响应式重取</td></tr><tr><td>字段脱敏规则</td><td>后端 <code>SysFieldLevelSecurity</code></td><td><strong>脱敏在服务端完成</strong>，前端只用规则控制只读/标识</td></tr><tr><td>站点品牌</td><td>后端 <code>/Auth/UserInfo</code> 的 <code>appTitle</code> / <code>appLogo</code></td><td>登录后写入 app store，覆盖 <code>.env</code> 兜底</td></tr></tbody></table><p>因此<strong>新增页面的主要工作在后端</strong>（<code>PageRegistry</code> + 权限），前端只补视图组件、API 客户端与 i18n 文案。</p><h2 id="相关页面" tabindex="-1">相关页面 <a class="header-anchor" href="#相关页面" aria-label="Permalink to &quot;相关页面&quot;">​</a></h2><ul><li><a href="./development">开发流程</a>：新增一个页面的五步与自检清单</li><li><a href="./../frontend/schema-page">Schema 驱动页面</a>：列表页开发手册</li><li><a href="./../frontend/routing">路由与菜单</a>：动态路由生成与守卫</li><li><a href="./../frontend/permission">权限与脱敏</a>：三级过滤与 FLS</li><li><a href="./theme">布局与主题</a>：布局、Tailwind v4、主题、偏好同步</li><li><a href="./i18n">国际化</a>：语言包与文案</li><li><a href="./../api-guide">接口对接指南</a>：响应信封与请求协议</li></ul>`,32)])])}const g=a(d,[["render",i]]);export{k as __pageData,g as default};
